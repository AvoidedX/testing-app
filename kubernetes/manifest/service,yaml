apiVersion: v1
kind: Service
metadata:
  name: welcome-service
spec:
  type: ClusterIP
  selector:
    app: welcome-app
  ports:
    - name: http
      port: 80
      targetPort: 5000
      protocol: TCP
